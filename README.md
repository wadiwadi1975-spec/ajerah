# تطبيق «أُجْرَه» (Ajarah)

منصة توصيل ركاب داخل الجغرافيا السورية، تحاكي فكرة تطبيق أوبر (Uber) ولكن مخصّصة للمدن والطرق السورية.

## الهدف
- ربط الركاب بالسائقين داخل سوريا.
- حساب المسافات والأجور بناءً على جغرافيا المدن السورية (دمشق، حلب، حمص، حماه، اللاذقية، إدلب، درعا، دير الزور، الرقة، الحسكة، القامشلي، طرطوس، السويداء، القنيطرة...).
- واجهة عربية RTL مريحة.

## الحالة الحالية
نموذج أولي (Prototype) بصفحة ويب بسيطة لعرض فكرة التطبيق. سيتم لاحقاً تحديد التقنية النهائية (تطبيق جوال Flutter/React Native، أم موقع ويب).

## التشغيل
افتح ملف `index.html` في المتصفح.

## الخطوات القادمة (مقترحة)
- [ ] تحديد التقنية (ويب / أندرويد / iOS).
- [x] دمج خرائط حقيقية (Leaflet + OpenStreetMap) مع تركيز على سوريا.
- [x] شاشة طلب رحلة (انطلاق → مطار دمشق → أقرب سيارة ضمن 1 كم حول موقعك الدقيق).
- [x] محرّك أجر بنوعي العملة (ل.س / دولار) حسب سعر الصرف.
- [x] صفحة تسجيل السائقين (`register.html`) مع شروط التسجيل.
- [x] آلية التسجيل والدخول (`auth.html` — راكب/سائق) مع حفظ محلي.
- [ ] لوحة السائق ولوحة الراكب.
- [ ] الحسابات والمحافظ.

## آلية التسجيل في التطبيق
الملف `auth.html` هو نقطة الدخول الموحدة:

### الأدوار
- **راكب**: الاسم + الهاتف (أي رقم دولي/محلي — 7-15 رقم) + كلمة مرور (≥6).
- **سائق**: نفس بيانات الراكب + بيانات المركبة (النوع، الموديل، سنة الصنع ≤10 سنوات، اللوحة).

### التخزين (نموذج أولي)
- `localStorage.ajrah_users` — مصفوفة المستخدمين `{id, name, phone, pass, role, vehicle?, createdAt}`.
- `localStorage.ajrah_current` — المستخدم الحالي بعد تسجيل الدخول.
- `localStorage.ajrah_wallet_due` — رصيد العمولات النقدية المتراكمة على السائق (ل.س).
- `localStorage.ajrah_rides` — سجل الرحلات `{at, driver, type, fare, fee, pay}`.
- الهاتف هو المعرّف الفريد؛ يمنع التكرار.
- طلب الرحلة في `index.html` يتطلب تسجيل دخول، وإلا يحوّل إلى `auth.html`.
- لاحقاً تستبدل بـ API + قاعدة بيانات + تحقق OTP عبر SMS، وتشفير كلمة المرور.

## آلية الربح (مُعتمدة)
- **الدفع الإلكتروني:** يخصم النظام **15%** (`COMMISSION_RATE`) تلقائياً ويحوّل الصافي للسائق.
- **الدفع النقدي:** يحتفظ السائق بكامل الأجر نقداً، وتتراكم العمولة (15% من كل رحلة) في `ajrah_wallet_due`. عند وصولها **200,000 ل.س** (`WALLET_THRESHOLD`) يُطلب التسديد عبر الشحن وإلا يُعلّق الظهور ضمن 1 كم.
- **الاشتراك الاختياري (قريباً):** للمحترفين عمولة مخفضة 10% ومزايا ظهور.
- صفحة `wallet.html` تعرض الرصيد والسجل وزر التسديد (محاكاة).

## تسجيل السائقين (الأساس)
صفحة `register.html` هي الأساس لتسجيل السائق لمركبته، وتتحقق من الشروط التالية:

### شروط السائق
1. العمر 21 سنة فأكثر.
2. رخصة قيادة سارية مضى عليها سنة على الأقل.
3. بطاقة شخصية/هوية سورية سارية.
4. سجل أمني نظيف (إفادة بعدم وجود سوابق).
5. صورة شخصية واضحة وحديثة.
6. اجتياز المقابلة/التقييم من إدارة التطبيق.

### شروط المركبة
1. سنة الصنع لا تزيد عن 10 سنوات (`MAX_AGE_LIMIT`).
2. فحص فني دوري ساري.
3. تأمين إلزامي ساري.
4. لوحات سورية مسجّلة باسم السائق أو بتفويض رسمي.
5. صالحة لنقل الركاب (سلامة فرامل/إطارات/إضاءة، عدد مقاعد لا يقل عن 4).
6. صور واضحة للمركبة من الأمام والخلف ورقم اللوحة.

### البيانات المجمّعة (Payload)
عند الإرسال يُبنى كائن:
```
{
  driver:  { name, id, birth, phone, city, license, licenseExpiry },
  vehicle: { type, model, year, plate, fuel, seats, insuranceExpiry, inspectionDate }
}
```
يُرسل حالياً إلى `console.log` (نموذج أولي بدون خادم). لاحقاً يُربط بقاعدة بيانات/خادم (API).

